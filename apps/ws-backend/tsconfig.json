// imported the base typescript configuration from the common package
// added root and outdir to facilitate build script
{
    "extends" : "@repo/typescript-config/base.json",
    "compilerOptions": {
        "rootDir": "./src",
        "outDir": "./dist"
    }
}